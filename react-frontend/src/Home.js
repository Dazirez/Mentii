import React from "react";
import { Grid } from "@material-ui/core";
const Home = () => {
    return (
        <Grid container spacing={4}>
            <h1>Home</h1>
        </Grid>
    );
};

export default Home;
